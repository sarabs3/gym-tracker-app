import React from 'react';
import Input from '../../components/FormElements/Input';

const AddMachineType = () => {  
    return (
        <form>
            <Input onChange={(e) => {}} value='' id='title' name='title' />
        </form>
    );
}

export default AddMachineType;
